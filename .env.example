# Environment Variables for Chat Eval Application with Google SSO

# Google OAuth Configuration (Required)
# Get these from https://console.cloud.google.com/
GOOGLE_CLIENT_ID=your_google_client_id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your_google_client_secret

# Encryption Key for API Keys (Required)
# Generate with: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
ENCRYPTION_KEY=your_encryption_key_here

# Flask Configuration
FLASK_ENV=development
SECRET_KEY=your-secret-key-here

# Database Configuration (PostgreSQL)
# For Render: This will be automatically set
DATABASE_URL=postgresql://user:password@localhost:5432/chateval

# Redis Configuration (Optional)
# For Render: This will be automatically set if Redis addon is enabled
REDIS_URL=redis://localhost:6379

# Server Configuration
PORT=5001

# Note: Users bring their own Anthropic API keys
# No need for a global ANTHROPIC_API_KEY anymore